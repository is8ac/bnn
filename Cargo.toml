[package]
name = "bitnn"
version = "0.1.0"
authors = ["Isaac Leonard <ifleonar@us.ibm.com>"]
edition = "2018"
description = "Greedy layerwise generation of binary parameters for multi layer conv nets"
keywords = ["convolutional neural networks", "neural networks", "bitnets", "cifar10"]

[dependencies]
time = "0.1"
rand = "0.6"
rand_hc = "0.1.0"
rayon = "1.2"
num_cpus = "1.11"
serde = { version = "1.0", features = ["derive"] }
bincode = { version = "1.2", features = ["i128"] }

[profile.release]
#lto = true
